apiVersion: v1
kind: Secret
metadata:
  name: shopmate-secrets
  namespace: default
type: Opaque
stringData:
  # Ideally, use AWS Secrets Manager and ExternalDispatcher for production
  DB_PASSWORD: "shopmate_password"
  JWT_SECRET: "super_secret_key_change_me_in_prod"
  # Add other API keys here (e.g., STRIPE_KEY, AWS_ACCESS_KEY)
